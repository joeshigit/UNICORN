{
  "name": "unicorn-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test *.test.mjs",
    "test:pure": "node --test pure.logic.test.mjs",
    "test:rules": "node --test rules.test.mjs",
    "test:storage": "node --test storage.rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0"
  }
}
